class Solution {
    int findMaximum(int[] arr, int n) {
        // code here
        int temp = 0;
        Arrays.sort(arr);
        for(int i = 0; i < n; i++){
            temp = arr[n-1];
        }
        return temp;
    }
}
